## What to build

Finalize the remaining version-one product policy decisions and wire the chosen behaviors through the UI, backend behavior, and project scripts where needed.

## Acceptance criteria

- [x] The project finalizes behavior for default custom-model naming, failed-job visibility in version one, and whether setup should be separate from start.
- [x] The chosen behaviors are implemented end to end and reflected in project guidance.
- [x] Regression checks cover the finalized version-one policy behavior.

## Blocked by

- Blocked by #1
- Blocked by #5
